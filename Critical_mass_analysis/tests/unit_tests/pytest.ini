[pytest]
markers =
    input_values_test: testing input values.
    definition_test: testing if constructed functions are close to a defining benchmark.
    property_test : testing if constructed functions follow mathematical identities.